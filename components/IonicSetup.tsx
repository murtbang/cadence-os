'use client';

import { setupIonicReact } from '@ionic/react';
import '@ionic/core/css/core.css';
import '@ionic/core/css/ionic.bundle.css';

// Initialize Ionic web components once, client-side only
setupIonicReact({
  mode: 'md', // Material Design mode — consistent across Android
});

export default function IonicSetup() {
  return null;
}
